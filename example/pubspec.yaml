name: curved_app_bar_example
description: Example app for testing the curved_app_bar package locally.
publish_to: none

environment:
  sdk: ^3.12.1

dependencies:
  curved_app_bar:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: any
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
